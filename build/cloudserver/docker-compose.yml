version: '3.8'
services:
  restapi:
    image: cloudserver_restapi:latest
    environment:
      - DB_HOST=192.168.1.147
      - DB_USERNAME=restapi
      - DB_PASSWORD
      - DB_NAME=restapi
    ports:
      - "8080:8080"