services:
  restapi:
    image: cloudserver:latest
    environment:
      DB_HOST: 192.168.1.147
      DB_USERNAME: restapi
      DB_PASSWORD: ${DB_PASSWORD?err}   # fail if missing
      DB_NAME: restapi
    expose:
      - "8080"

  nginx:
    image: nginx:1.27-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro

      # Webroot for ACME HTTP-01 challenges
      - certbot-webroot:/var/www/certbot

      # Shared certs between Nginx and Certbot
      - letsencrypt:/etc/letsencrypt
    depends_on:
      - certbot
    restart: unless-stopped

  certbot:
    image: certbot/certbot:latest
    volumes:
      - certbot-webroot:/var/www/certbot
      - letsencrypt:/etc/letsencrypt
    entrypoint: >
      sh -c 'trap exit TERM; 
             while :; do 
               certbot renew --webroot -w /var/www/certbot --quiet; 
               sleep 12h; 
             done'
    restart: unless-stopped

volumes:
  certbot-webroot:
  letsencrypt: